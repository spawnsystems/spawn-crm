/** Cookie name used by platform admins to preview a specific tenant. */
export const PREVIEW_COOKIE = 'preview_tenant_id'

/** Default page sizes for paginated tables. */
export const DEFAULT_PAGE_SIZE = 50

/** Hours of inactivity before a lead is flagged `at_risk`. */
export const AT_RISK_HOURS = 24

/** Days of inactivity before a lead is moved to the rescue queue. */
export const ABANDONED_DAYS = 15
